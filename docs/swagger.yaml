info:
  contact: {}
paths:
  /:
    get:
      consumes:
      - application/json
      description: do test
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: index sample
      tags:
      - example
swagger: "2.0"
